import cdy from "./cdy"


console.log(cdy("在此之前使用我们通常获取一个多层级对象一个比较内层的属性，为了避免为空的情况不得不这样写\n"))
console.log(cdy("在此之前使用我们通常获取一个多层级对象一个比较内层的属性，为了避免为空的情况不得不这样写\n"))
console.log(cdy("在此之前使用我们通常获取一个多层级对象一个比较内层的属性，为了避免为空的情况不得不这样写\n"))
console.log(cdy("在此之前使用我们通常获取一个多层级对象一个比较内层的属性，为了避免为空的情况不得不这样写\n"))
